<Window x:Class="WindowsClient.App.Views.ProfileWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Profile Wizard"
        Height="520"
        Width="760">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="20" FontWeight="SemiBold" Text="Profile Wizard" />
        <ListBox Grid.Row="2" ItemsSource="{Binding Steps}" SelectedIndex="{Binding CurrentStep}" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="88" Margin="0,0,8,0" Content="Back" />
            <Button Width="88" Margin="0,0,8,0" Content="Next" />
            <Button Width="88" Content="Save" />
        </StackPanel>
    </Grid>
</Window>
